meta:
  toolchain:
    name: docker
  environment: docker

build:
  dockerfilePath: DiagnostMathBot/math_diagnostic_bot/Dockerfile
  buildContext: DiagnostMathBot/math_diagnostic_bot

run:
  persistenceMount: /app/data
