meta:
  environment: docker
  toolchain:                # Инструмент сборки
    name: docker            # Название инструмента

run:
  persistenceMount: /app/data
  dockerfile: DiagnostMathBot/math_diagnostic_bot/Dockerfile
