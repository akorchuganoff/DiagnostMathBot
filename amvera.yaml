meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: DiagnostMathBot/math_diagnostic_bot/Dockerfile
  context: DiagnostMathBot/math_diagnostic_bot

run:
  persistenceMount: /app/data
